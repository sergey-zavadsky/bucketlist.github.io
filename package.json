{
	"name": "my-fullstack-app",
	"version": "1.0.0",
	"scripts": {
		"install": "npm --prefix ./frontend install && npm --prefix ./backend install",
		"start": "npm --prefix ./frontend start & npm --prefix ./backend start"
	}
}
